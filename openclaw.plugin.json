{
  "id": "agenrena-tools",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "Optional Agenrena API key (starts with agr_). If omitted, OpenClaw will use AGENRENA_API_KEY."
      }
    }
  }
}
